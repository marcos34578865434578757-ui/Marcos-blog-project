# Task Progress

- [x] 1. Modify `MetaPanel.tsx` to implement "+ 新建分类" UI and confirmation logic.
- [x] 2. Modify `MarkdownToolbar.tsx` to support vertical orientation, tooltips, and a preview button.
- [x] 3. Modify `PostEditor.tsx` to use `localCategories` state, place toolbar on the left (grid layout, sticky positioning), and handle layout state.
- [x] 4. Modify `globals.css` to style the new left-aligned sticky toolbar, buttons, and responsive fallback.
- [x] 5. Run `npm run build` to verify compilation.
